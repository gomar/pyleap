Bench_algo
============

Two scripts are given:
- bench_algo.py: the python script that gives the results
- bench_algo.gnuplot: script to generate figures

To reproduce the figures, pyleap must be correctly installed and then:
- python bench_algo.py (wait to finish),
- gnuplot bench_algo.gnuplot
- latex BENCH_ALGO_PA.tex
  dvips -P pdf -E BENCH_ALGO_PA.dvi -o BENCH_ALGO_PA.eps
  epstopdf BENCH_ALGO_PA.eps
- latex BENCH_ALGO_EQUI.tex
  dvips -P pdf -E BENCH_ALGO_EQUI.dvi -o BENCH_ALGO_EQUI.eps
  epstopdf BENCH_ALGO_EQUI.eps